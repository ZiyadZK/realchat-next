import { Inconsolata, Nunito_Sans, Quicksand } from "next/font/google";

export const quick = Quicksand({subsets: ['latin']});
export const nunito = Nunito_Sans({subsets: ['latin']})
export const inco = Inconsolata({subsets: ['latin']})